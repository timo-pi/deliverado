<div id="mapNav"
  style="width: 100vw;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>" >
</div>

<div class="finish button">
  <%= link_to "finish", edit_delivery_path(@delivery), class: "btn btn-primary form-btn"  %>
</div>
